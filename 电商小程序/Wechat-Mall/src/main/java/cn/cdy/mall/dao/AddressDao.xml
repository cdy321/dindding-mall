<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cdy.mall.dao.AddressDao">
    <insert id="insertAddressInfo" keyProperty="id" useGeneratedKeys="true">
        insert into address(name, phoneNum, district, detailaddress, floor, beizhu, userid)
        values (#{name},#{phoneNum},#{district},#{detailaddress},#{floor},#{beizhu},#{userid})
    </insert>
</mapper>